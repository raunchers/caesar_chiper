<!DOCTYPE html>
<html>
    <head>
        <style>
            body{
                display: flex;
                flex-direction: column;
                align-items: center;
                background-color: #666261;
                font-size: 20px;
            }
            .container{
                display: grid;
                grid-template: repeat(2,1fr) / repeat(1,1fr);
                grid-gap: 2px;
            }
            form{
                display: grid;
                grid-gap: 2px;
            }
            .shift{
                max-width: 2em;
            }
            input.button{
                max-width: 6em;
                height: 30px;
                font-size: 15px;
                font-weight: bold;
            }
            input{
                border-radius: 2px;
                font-size: 28px;
            }
            .outputString{
                border: 1px solid;
                padding: 5px;
                box-shadow: 5px 5px;
                max-width: 80%;
            }
        </style>
    </head>
        <body>
            <div class="container">
                <form>
                    <input type="text" class="sentence" name="sentence" value="Enter a sentence">
                    <input type="number" class="shift" name="shift" min="0" max="100" value="0">
                    <input type="submit" class="button" value="encrpyt">
                </form>
                <div class="outputString">
                    <%= "#{garbled_string}" %>
                </div>
            </div>
        </body>
</html>